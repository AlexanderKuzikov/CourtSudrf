# BUG_REPORT — CourtSudrf v0.1.0

> Обновлён: 2026-07-12. Исправлено: 13 из 15 багов.

## Легенда

| Метка | Значение |
|-------|---------|
| ~~B1~~ | Исправлено |
| **B2** | Открыто |

---

## Критические

### ~~B1. `all` command — сломана~~ ✅ Исправлено
**Файл:** `src/index.ts:267-285`
**Было:** `program.parseAsync()` для вызова подкоманд.
**Стало:** Логика `fetch` и `details` вынесена в отдельные async-функции `runFetch()` и `runDetails()`, которые `all` вызывает напрямую.

### ~~B2. `all` command — parseInt без лямбды~~ ✅ Исправлено
**Файл:** `src/index.ts:270-271`
**Было:** `.option(..., parseInt)` — radix путается Commander.
**Стало:** `.option(..., (v) => parseInt(v, 10))`.

## Серьёзные

### ~~B3. `ms` команда — Bottleneck с minTime=0~~ ✅ Исправлено
**Файл:** `src/index.ts:304,313`
**Было:** `new SudrfClient(0)`.
**Стало:** `new SudrfClient(100)` — 100ms безопасно для single запроса.

### ~~B4. `ms` команда — два экземпляра SudrfClient~~ ✅ Исправлено
**Файл:** `src/index.ts:304,313`
**Было:** Два клиента (один для HTTP, второй для парсинга).
**Стало:** `SudrfClient.parseMsHtml()` — статический метод, не требует инстанса.

### ~~B5. Привязка телефона к ПСП~~ ✅ Исправлено (частично)
**Файл:** `src/core/SudrfClient.ts:213`
**Было:** Телефон только на первом адресе.
**Стало:** Телефон на всех адресах (полноценный парсинг с привязкой к ПСП — в разработке).

### ~~B6. `ya_coords` название затирает название из `ya_info`~~ ✅ Исправлено
**Файл:** `src/index.ts:216-218`
**Было:** `if (entry && entry.name) { record.name = entry.name; }`
**Стало:** Приоритет у `ya_info` (свежий источник). Код перезаписи удалён.

## Незначительные

### ~~B7. Неиспользуемая зависимость `axios-retry`~~ ✅ Исправлено
**Было:** В `package.json`.
**Стало:** Удалена из зависимостей.

### ~~B8. Мёртвый код: `getCourtCodes()`~~ ✅ Исправлено
**Было:** В `SudrfClient.ts`.
**Стало:** Метод удалён.

### ~~B9. Мёртвый код: `extractPrefix()`~~ ✅ Исправлено
**Было:** В `CourtRecord.ts`.
**Стало:** Функция удалена.

### ~~B10. Мёртвый код: `RegionInfo`~~ ✅ Исправлено
**Было:** В `types/sudrf.ts`.
**Стало:** Интерфейс удалён. `YaInfoHtml.okrugName` по-прежнему парсится (может пригодиться).

### ~~B11. Отсутствует LICENSE файл~~ ✅ Исправлено
**Было:** Файла нет.
**Стало:** `LICENSE` (Apache 2.0) добавлен в корень проекта.

### ~~B12. `data/.progress` не очищается~~ ✅ Исправлено
**Файл:** `src/index.ts:250`
**Было:** Файл остаётся после завершения.
**Стало:** `clearProgress()` вызывается, если обработаны все запрошенные коды.

### ~~B13. Регулярка парсинга MS HTML хрупкая~~ ✅ Исправлено
**Файл:** `src/core/SudrfClient.ts:295-296`
**Было:** `/a[^>]*href='([^']+)'` — первая ссылка в блоке.
**Стало:** `/target='_blank'\s+href='(https?:\/\/[^']+)'` — конкретно ссылка на сайт.

### B14. Жёстко закодированный список регионов ⏳ Отложено
**Файл:** `src/index.ts:349-441`
**Статус:** Парсинг из HTML-селекта потребует отдельного эндпоинта. Сейчас не критично.

### B15. Нет тестов ⏳ Отложено
**Статус:** Проект в стадии прототипа. Тесты появятся после стабилизации архитектуры.

---

**Итого:** 15 багов: 2 крит. ✅, 4 серьёз. ✅, 9 незнач. → 2 отложено, 13 исправлено.
